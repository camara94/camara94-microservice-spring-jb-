import { MathDirectiveDirective } from './math-directive.directive';

describe('MathDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MathDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
